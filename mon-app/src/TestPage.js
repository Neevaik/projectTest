function TestPage({ goToHome }) {
    return (
        <div>
            <button onClick={goToHome}>Retour à Home</button>

            <div>
                {/* ici */}
            </div>

        </div>
    );
}

export default TestPage;
